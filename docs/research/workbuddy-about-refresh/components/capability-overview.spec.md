# Capability Overview Specification

- Target: `CapabilityOverviewContent`.
- Interaction: click navigation and card hover elevation.
- Five cards use the existing local SVG capability icons, 19/600 headings, and 15px muted descriptions.
- Desktop layout is 3+2 centered; tablet is two columns; mobile is one column.
- Links remain local: `/capability/management`, `/test`, `/inspection`, `/tech`, `/smart`.
